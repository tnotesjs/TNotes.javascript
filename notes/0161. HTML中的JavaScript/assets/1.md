# 注意事项

- VitePress 把 Markdown 转成 Vue 模板时，解析器会把 `<script></script>` 它们当成模板标签处理，导致编译错误。
- 临时的解决方案：将涉及到 `<script></script>` 的内容放入独立文件中，通过 VitePress 的 `<<<` 语法引入到笔记中。
- 这是一个书写时需要注意的问题：正文中尽量用纯文本描述 script 标签；如果确实要展示完整标签源码，可以考虑放到独立文件里再引入，避免 Vue 模板解析器误判。
