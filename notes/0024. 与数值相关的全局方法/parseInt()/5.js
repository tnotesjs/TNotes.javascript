console.log(parseInt('1000')) // 1000
console.log(parseInt('1000', 10)) // 1000
// 上述两种写法等效。

// parseInt 方法还可以接受第二个参数（2 到 36 之间），
// 表示被解析的值的进制，返回该值对应的十进制数。

// 默认情况下，parseInt 的第二个参数为 10，即默认是十进制转十进制。