var s = 'hello'
var arr = ['h', 'e', 'l', 'l', 'o']

console.log(s[0]) // "h"
console.log(s[1]) // "e"
console.log(s[4]) // "o"

console.log(arr[0]) // "h"
console.log(arr[1]) // "e"
console.log(arr[4]) // "o"

// 字符串可以被视为字符数组，因此可以使用数组的方括号运算符，
// 用来返回某个位置的字符（位置编号从 0 开始）。